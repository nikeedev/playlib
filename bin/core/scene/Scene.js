/**
 * @description Creating a scene that can later be added to a game.
 *
 * @since 0.6.0
*/
class Scene {
    ClearScreen = false;
    create(ctx) {
    }
    update(ctx, deltaTime) {
    }
}
export { Scene };
