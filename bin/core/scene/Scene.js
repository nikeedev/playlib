/**
 * @description Creating a scene that can later be added to a game.
 *
 * @since 0.6.0
*/
class Scene {
    canvas;
    ClearScreen;
    constructor(canvas) {
        this.canvas = canvas;
    }
    create(ctx) {
    }
    update(ctx) {
    }
}
export { Scene };
